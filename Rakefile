require 'rubygems'
require 'rake'
require 'echoe'

Echoe.new('git-deploy', '0.0.1') do |p|
  p.url            = "http://github.com/scottbnel/git-deploy"
  p.author         = "Scott Nelson"
  p.email          = "scottbnel@gmail.com"
  p.development_dependencies = ["net-sftp", "grit"]
end